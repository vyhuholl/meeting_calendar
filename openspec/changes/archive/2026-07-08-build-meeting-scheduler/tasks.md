## 1. Каркас проекта и БД

- [x] 1.1 Создать пакет `app/` с `__init__.py` и структуру `app/routers/`
- [x] 1.2 `app/config.py`: настройки, `DATABASE_URL` из env (дефолт — локальный файл SQLite), TZ-соглашения
- [x] 1.3 `app/db.py`: `engine`, `SessionLocal`, `Base`, зависимость `get_db()`
- [x] 1.4 Добавить недостающие зависимости через `uv add` (`uvicorn` для запуска API, HTTP-клиент `httpx` для UI); прогнать `uv sync`

## 2. Модели данных

- [x] 2.1 `app/models.py`: `Employee(id, name)`
- [x] 2.2 `app/models.py`: `Meeting(id, title, starts_at, ends_at)` — время в UTC
- [x] 2.3 `app/models.py`: ассоциативная таблица `meeting_participants(meeting_id, employee_id)` с FK на обе стороны (M2M)

## 3. Логика пересечений (ядро)

- [x] 3.1 `app/conflicts.py`: чистая функция `intervals_overlap(s1, e1, s2, e2) -> bool` = `s1 < e2 and s2 < e1` (полуоткрытые интервалы)
- [x] 3.2 `app/conflicts.py`: функция поиска конфликтующих встреч по набору участников и интервалу (кандидаты через SQL `starts_at < :new_end AND :new_start < ends_at`)
- [x] 3.3 `tests/test_conflicts.py`: граничные случаи — впритык (нет конфликта), одинаковые границы, вложенный интервал, частичное перекрытие слева и справа, непересекающиеся

## 4. Схемы (Pydantic)

- [x] 4.1 `app/schemas.py`: `EmployeeOut`
- [x] 4.2 `app/schemas.py`: `MeetingCreate` с валидацией `starts_at < ends_at` и «минимум 2 участника»
- [x] 4.3 `app/schemas.py`: `MeetingOut` (с участниками) и модель тела ошибки-конфликта (кто и когда занят)

## 5. CRUD и создание встречи

- [x] 5.1 `app/crud.py`: получить список сотрудников
- [x] 5.2 `app/crud.py`: получить расписание за интервал с опциональным фильтром по `employee_id` (включать встречи, пересекающиеся с окном)
- [x] 5.3 `app/crud.py`: транзакционное создание встречи — проверка существования участников, проверка конфликтов и вставка в одной транзакции; при конфликте — rollback и структурированная ошибка

## 6. Роуты API

- [x] 6.1 `app/routers/employees.py`: `GET /employees`
- [x] 6.2 `app/routers/meetings.py`: `POST /meetings` → 201 при успехе, 409 при конфликте (кто и когда), 422 при невалидном вводе
- [x] 6.3 `app/routers/meetings.py`: `GET /meetings` расписание на день/неделю с параметрами периода и опциональным `employee_id`
- [x] 6.4 `app/main.py`: создать `FastAPI`, `Base.metadata.create_all(engine)` на старте, идемпотентный seed сотрудников, подключить роутеры

## 7. Streamlit UI (тонкий клиент)

- [x] 7.1 `ui/app.py`: чтение `API_BASE_URL` из env (дефолт `http://localhost:8000`), HTTP-клиент к API
- [x] 7.2 `ui/app.py`: выбор «кто я» — выпадающий список из `GET /employees`
- [x] 7.3 `ui/app.py`: форма создания встречи (title, дата/время начала и конца, мультиселект участников); показ 409 человекочитаемо (кто и когда занят)
- [x] 7.4 `ui/app.py`: просмотр расписания на день/неделю с фильтром по сотруднику; отображение времени в локальной TZ

## 8. Тесты API

- [x] 8.1 `tests/test_meetings.py`: успешное создание встречи (201) и присутствие в расписании
- [x] 8.2 `tests/test_meetings.py`: отказ 409 при пересечении у одного из участников; сообщение называет сотрудника и время
- [x] 8.3 `tests/test_meetings.py`: встречи впритык не конфликтуют (201)
- [x] 8.4 `tests/test_meetings.py`: валидация — меньше 2 участников и `starts_at >= ends_at` → 422
- [x] 8.5 `tests/test_meetings.py`: расписание за день/неделю и фильтр по сотруднику

## 9. Контейнеризация и финализация

- [x] 9.1 Дополнить контейнеризацию для запуска UI отдельным процессом (`streamlit run ui/app.py`); при желании — `docker-compose` для API+UI
- [x] 9.2 Обновить `README.md`: как запустить API и UI (локально и в Docker), переменные окружения
- [x] 9.3 Прогнать `make validate` (ruff + mypy) и устранить замечания
- [x] 9.4 Прогнать `make test` (pytest с покрытием) и убедиться, что все тесты зелёные
