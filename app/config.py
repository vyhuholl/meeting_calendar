"""Настройки приложения.

Время всегда храним и сравниваем в UTC (naive UTC в БД); в UI показываем в
локальной таймзоне.
"""

from __future__ import annotations

import os

# Строка подключения к БД. По умолчанию — локальный файл SQLite.
DATABASE_URL: str = os.getenv("DATABASE_URL", "sqlite:///./meeting_calendar.db")

# Предопределённый набор сотрудников для идемпотентного наполнения пустой БД,
# чтобы приложением можно было пользоваться сразу.
SEED_EMPLOYEES: tuple[str, ...] = (
    "Алиса",
    "Борис",
    "Виктор",
    "Галина",
    "Дмитрий",
)
